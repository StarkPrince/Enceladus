{
  "version": 2,
  "builds": [
    {
      "src": "Django",
      "use": "@vercel/python"
    }
  ],
  "routes": [
    {
      "src": "/(.*)",
      "dest": "/$1"
    }
  ],
  "env": {
    "DATABASE_URL": "${DATABASE_URL}"
  }
}
